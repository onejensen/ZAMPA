{
  "name": "zampa-web-functions",
  "private": true,
  "description": "Sirve /o y /r de getzampa.com desde Firebase Hosting con las mismas Pages Functions de /functions",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
